# go-cloud-datastore-sort-indexes

index.yaml の diff に困っているあなたへ

## Usage

```sh
go install github.com/s-aska/go-cloud-datastore-sort-indexes
go-cloud-datastore-sort-indexes < ./index.yaml > index_sorted.yaml
```
